## Packages
framer-motion | Smooth animations for results and transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
API endpoint: POST /api/analyze expects multipart/form-data
Payload fields: 'image' (File) and 'language' ('en' | 'hi')
